# Generated by Django 5.2.17 on 2026-09-24 18:19

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    initial = True

    dependencies = [
    ]

    operations = [
        migrations.CreateModel(
            name='Author',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('name', models.CharField(help_text='The name as it should appear on the catalogue entry.', max_length=200)),
            ],
            options={
                'ordering': ['name'],
            },
        ),
        migrations.CreateModel(
            name='Member',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('name', models.CharField(help_text='The name the member uses at the desk.', max_length=200)),
                ('email', models.EmailField(help_text='Used to tell two members with the same name apart. One address per member.', max_length=254, unique=True)),
                ('joined_on', models.DateField(help_text='The day the member joined the library.')),
            ],
            options={
                'ordering': ['name'],
            },
        ),
        migrations.CreateModel(
            name='Book',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('title', models.CharField(help_text='The title printed on the title page, without a subtitle unless the subtitle is how readers look it up.', max_length=300)),
                ('isbn', models.CharField(blank=True, help_text='ISBN-13, with or without hyphens. Leave blank only when the copy has no ISBN.', max_length=17, null=True, unique=True, verbose_name='ISBN')),
                ('copies', models.PositiveIntegerField(default=1, help_text='How many physical copies the library owns, including copies that are currently on loan.')),
                ('cover', models.ImageField(blank=True, help_text='A JPEG, PNG, or WebP of the front cover. Used on the book list.', upload_to='covers/')),
                ('authors', models.ManyToManyField(help_text='Every person named as an author. Add the author record first if it is not in the list.', related_name='books', to='library.author')),
            ],
            options={
                'ordering': ['title'],
            },
        ),
        migrations.CreateModel(
            name='Loan',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('borrowed_on', models.DateField(help_text='The day the member took the book.')),
                ('due_on', models.DateField(help_text='The day the book is expected back. Must be on or after the day it was borrowed.')),
                ('returned_at', models.DateTimeField(blank=True, help_text='When the book came back. Leave empty while the loan is still open.', null=True)),
                ('book', models.ForeignKey(help_text='The book that left the shelf. A book with loans cannot be deleted.', on_delete=django.db.models.deletion.PROTECT, related_name='loans', to='library.book')),
                ('member', models.ForeignKey(help_text='The member who has the book. A member with loans cannot be deleted.', on_delete=django.db.models.deletion.PROTECT, related_name='loans', to='library.member')),
            ],
            options={
                'ordering': ['due_on', 'id'],
            },
        ),
        migrations.AddConstraint(
            model_name='book',
            constraint=models.CheckConstraint(condition=models.Q(('copies__gte', 1)), name='book_copies_at_least_one'),
        ),
        migrations.AddIndex(
            model_name='loan',
            index=models.Index(fields=['book', 'returned_at'], name='loan_book_returned_idx'),
        ),
        migrations.AddConstraint(
            model_name='loan',
            constraint=models.CheckConstraint(condition=models.Q(('due_on__gte', models.F('borrowed_on'))), name='loan_due_on_not_before_borrowed_on'),
        ),
    ]
